# Test-/Lint-Abhängigkeiten (pytest etc. — im selben Image installiert,
# damit `docker compose run api pytest` funktioniert)
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
flake8>=7.0
